---
variant: "fcos"
version: "1.5.0"

kernel_arguments:
  should_exist:
    - "crashkernel=256M"

storage:
  files:
    - path: "/etc/hostname"
      mode: 0644
      contents:
        inline: "syringe-test"

# vim: ft=yaml
